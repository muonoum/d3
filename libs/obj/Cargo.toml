[package]
name = "obj"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1.0.95"
array = { version = "0.1.0", path = "../array" }
image = { version = "0.25.5", default-features = false, features = ["jpeg", "png", "tga"] }
matrix = { version = "0.1.0", path = "../matrix" }
